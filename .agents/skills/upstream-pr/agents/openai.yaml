interface:
  display_name: "Contribute Upstream"
  short_description: "Prepare a focused Dispatch upstream contribution"
  default_prompt: "Turn this fork change into a focused, validated upstream Dispatch pull request."
