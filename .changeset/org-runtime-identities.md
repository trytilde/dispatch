---
"@trytilde/sdk": minor
"@trytilde/api-client": minor
---

Add organization-owned runtime identities, managed account linking, immutable per-request identity/team clients, and a server-only session-authorized runtime proxy. Regenerate the API contract for account-based Core billing and organization proxy tokens. Application credentials stay on the server and cannot acquire account administration through runtime delegation.
