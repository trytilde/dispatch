---
"@tryopenbot/mobile": minor
---

Name the app store identity for its publisher: Tilde publishes OpenBot, so the bundle identifier and Android package move from `dev.openbot.mobile` to `ai.trytilde.openbot` before any store submission, where the identifier becomes permanent. `openbot init` remains free of EAS questions and requirements.
