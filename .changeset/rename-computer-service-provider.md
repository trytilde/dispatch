---
"@trytilde/dispatch-computer-service-provider": minor
---

Rename the Computer lifecycle package to clarify that it builds, deploys, and provisions the Computer service, and remove the `computer-tools` compatibility re-export so agent runtime tools remain independently owned.
