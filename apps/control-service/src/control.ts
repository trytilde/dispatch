import type { ConnectRouter } from "@connectrpc/connect";

/** Register owner-facing control services as the UX contract is designed. */
export function registerControlServices(_router: ConnectRouter): void {}
